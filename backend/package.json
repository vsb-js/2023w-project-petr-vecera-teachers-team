{
  "name": "backend",
  "version": "1.0.0",
  "description": "This folder shoudl contain the backend code with Express. \r It should have it's own package.json and node_modules folder.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "prettier": "prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "prisma": "^5.6.0"
  }
}
